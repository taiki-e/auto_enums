name: CI

permissions:
  contents: read

on:
  pull_request:
  push:
    branches:
      - main
      - staging
      - v[0-9]+.[0-9]+
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

env:
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  CARGO_NET_GIT_FETCH_WITH_CLI: true
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTFLAGS: -D warnings
  RUSTUP_MAX_RETRIES: 10

defaults:
  run:
    shell: bash

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        rust:
          - stable
          - beta
          - nightly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - uses: taiki-e/github-actions/install-rust@main
        with:
          toolchain: ${{ matrix.rust }}
          component: rustfmt
      - name: Pre Cache cargo-expand
        run: |
          mkdir -p .github/caching
          curl -fsSL --retry 10 --retry-connrefused https://api.github.com/repos/dtolnay/cargo-expand/releases/latest \
            | jq -r '.tag_name' > .github/caching/cargo-expand.lock
          echo "${{ runner.tool_cache }}/cargo-expand/bin" >> "${GITHUB_PATH}"
        if: startsWith(matrix.rust, 'nightly')
      - name: Cache cargo-expand
        id: cache-cargo-expand
        uses: actions/cache@v2
        with:
          path: ${{ runner.tool_cache }}/cargo-expand/bin
          key: cargo-expand-bin-${{ hashFiles('.github/caching/cargo-expand.lock') }}
        if: startsWith(matrix.rust, 'nightly')
      - name: Install cargo-expand
        run: cargo install -f cargo-expand --debug --root ${{ runner.tool_cache }}/cargo-expand
        if: startsWith(matrix.rust, 'nightly') && steps.cache-cargo-expand.outputs.cache-hit != 'true'
      - run: cargo test --all --tests
      - run: cargo test --all --all-features
        if: startsWith(matrix.rust, 'nightly')

  build:
    strategy:
      matrix:
        range:
          # This is the minimum supported Rust version of this crate.
          # When updating this, the reminder to update the minimum supported
          # Rust version in README.md, Cargo.toml, and .clippy.toml.
          - 1.31..
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - run: cargo install cargo-hack --debug
      - run: cargo hack build --all --ignore-private --no-dev-deps --version-range ${{ matrix.range }} --version-step 2

  features:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - uses: taiki-e/github-actions/install-rust@main
      - run: cargo install cargo-hack --debug
      - run: cargo hack check --all --ignore-private --feature-powerset --depth 2 --no-dev-deps --features unstable --ignore-unknown-features

  tidy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - uses: taiki-e/github-actions/install-rust@main
        with:
          component: clippy,rustfmt
      - run: cargo install cargo-hack --debug
      - run: cargo fmt --all -- --check
        if: always()
      - run: cargo clippy --all --all-features --all-targets
        if: always()
      - run: tools/check-minimal-versions.sh
        if: always()
      - run: shellcheck $(git ls-files '*.sh')
        if: always()

  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - uses: taiki-e/github-actions/install-rust@main
      - run: cargo doc --no-deps --all --all-features
        env:
          RUSTDOCFLAGS: -D warnings

  # This job doesn't actually test anything, but they're used to tell bors the
  # build completed, as there is no practical way to detect when a workflow is
  # successful listening to webhooks only.
  #
  # ALL THE PREVIOUS JOBS NEEDS TO BE ADDED TO THE `needs` SECTION OF THIS JOB!

  ci-success:
    name: ci
    if: github.event_name == 'push' && success()
    needs:
      - test
      - build
      - features
      - tidy
      - docs
    runs-on: ubuntu-latest
    steps:
      - name: Mark the job as a success
        run: exit 0
